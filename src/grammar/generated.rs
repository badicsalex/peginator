// This file was generated by Peginator v0.2.6
// Hash of the grammar file: 639E081569A2B75ADC9C47381D547A77D2AB4B02E3535BCF7E37EC50F506EFD4
// Any changes to it will be lost on regeneration

#[derive(Debug, Clone)]
pub struct Grammar {
    pub rules: Vec<Grammar_rules>,
}
#[allow(non_camel_case_types)]
#[derive(Debug, Clone)]
pub enum Grammar_rules {
    CharRule(CharRule),
    ExternRule(ExternRule),
    Rule(Rule),
}
#[derive(Debug, Clone)]
pub struct Rule {
    pub directives: Vec<DirectiveExpression>,
    pub name: Identifier,
    pub definition: Choice,
}
#[derive(Debug, Clone)]
pub struct CharRule {
    pub directives: Vec<CheckDirective>,
    pub name: Identifier,
    pub choices: Vec<CharRulePart>,
}
#[allow(non_camel_case_types)]
#[derive(Debug, Clone)]
pub enum CharRulePart {
    CharRangePart(CharRangePart),
    CharacterRange(CharacterRange),
    Identifier(Identifier),
}
#[derive(Debug, Clone)]
pub struct ExternRule {
    pub directive: ExternDirective,
    pub name: Identifier,
}
#[derive(Debug, Clone)]
pub struct Choice {
    pub choices: Vec<Sequence>,
}
#[derive(Debug, Clone)]
pub struct Sequence {
    pub parts: Vec<DelimitedExpression>,
}
#[derive(Debug, Clone)]
pub struct Group {
    pub body: Choice,
}
#[derive(Debug, Clone)]
pub struct Optional {
    pub body: Choice,
}
#[derive(Debug, Clone)]
pub struct Closure {
    pub body: Choice,
    pub at_least_one: Option<AtLeastOneMarker>,
}
#[derive(Debug, Clone)]
pub struct AtLeastOneMarker;
#[derive(Debug, Clone)]
pub struct NegativeLookahead {
    pub expr: Box<DelimitedExpression>,
}
#[derive(Debug, Clone)]
pub struct PositiveLookahead {
    pub expr: Box<DelimitedExpression>,
}
#[derive(Debug, Clone)]
pub struct CharacterRange {
    pub from: CharRangePart,
    pub to: CharRangePart,
}
pub type CharRangePart = StringItem;
#[derive(Debug, Clone)]
pub struct StringLiteral {
    pub insensitive: Option<CaseInsensitiveMarker>,
    pub body: Vec<StringItem>,
}
#[derive(Debug, Clone)]
pub struct CaseInsensitiveMarker;
#[derive(Debug, Clone)]
pub struct Field {
    pub name: Option<Identifier>,
    pub boxed: Option<BoxMarker>,
    pub typ: Identifier,
}
#[derive(Debug, Clone)]
pub struct BoxMarker;
#[derive(Debug, Clone)]
pub struct OverrideField {
    pub typ: Identifier,
}
#[derive(Debug, Clone)]
pub struct IncludeRule {
    pub rule: Identifier,
}
#[allow(non_camel_case_types)]
#[derive(Debug, Clone)]
pub enum DelimitedExpression {
    CharacterRange(CharacterRange),
    Closure(Closure),
    EndOfInput(EndOfInput),
    Field(Field),
    Group(Group),
    IncludeRule(IncludeRule),
    NegativeLookahead(NegativeLookahead),
    Optional(Optional),
    OverrideField(OverrideField),
    PositiveLookahead(PositiveLookahead),
    StringLiteral(StringLiteral),
}
pub type Identifier = String;
pub type IdentifierChar = char;
#[allow(non_camel_case_types)]
#[derive(Debug, Clone)]
pub enum StringItem {
    HexaEscape(HexaEscape),
    SimpleEscape(SimpleEscape),
    Utf8Escape(Utf8Escape),
    char(char),
}
#[allow(non_camel_case_types)]
#[derive(Debug, Clone)]
pub enum SimpleEscape {
    SimpleEscapeBackslash(SimpleEscapeBackslash),
    SimpleEscapeCarriageReturn(SimpleEscapeCarriageReturn),
    SimpleEscapeDQuote(SimpleEscapeDQuote),
    SimpleEscapeNewline(SimpleEscapeNewline),
    SimpleEscapeQuote(SimpleEscapeQuote),
    SimpleEscapeTab(SimpleEscapeTab),
}
#[derive(Debug, Clone)]
pub struct SimpleEscapeNewline;
#[derive(Debug, Clone)]
pub struct SimpleEscapeCarriageReturn;
#[derive(Debug, Clone)]
pub struct SimpleEscapeTab;
#[derive(Debug, Clone)]
pub struct SimpleEscapeBackslash;
#[derive(Debug, Clone)]
pub struct SimpleEscapeQuote;
#[derive(Debug, Clone)]
pub struct SimpleEscapeDQuote;
#[derive(Debug, Clone)]
pub struct HexaEscape {
    pub c1: HexChar,
    pub c2: HexChar,
}
pub type HexChar = char;
#[derive(Debug, Clone)]
pub struct Utf8Escape {
    pub c1: HexChar,
    pub c2: Option<HexChar>,
    pub c3: Option<HexChar>,
    pub c4: Option<HexChar>,
    pub c5: Option<HexChar>,
    pub c6: Option<HexChar>,
}
#[allow(non_camel_case_types)]
#[derive(Debug, Clone)]
pub enum DirectiveExpression {
    CheckDirective(CheckDirective),
    ExportDirective(ExportDirective),
    LeftrecDirective(LeftrecDirective),
    MemoizeDirective(MemoizeDirective),
    NoSkipWsDirective(NoSkipWsDirective),
    PositionDirective(PositionDirective),
    StringDirective(StringDirective),
}
#[derive(Debug, Clone)]
pub struct StringDirective;
#[derive(Debug, Clone)]
pub struct CharDirective;
#[derive(Debug, Clone)]
pub struct NoSkipWsDirective;
#[derive(Debug, Clone)]
pub struct ExportDirective;
#[derive(Debug, Clone)]
pub struct PositionDirective;
#[derive(Debug, Clone)]
pub struct MemoizeDirective;
#[derive(Debug, Clone)]
pub struct LeftrecDirective;
#[derive(Debug, Clone)]
pub struct CheckDirective {
    pub name_parts: Vec<RustNamePart>,
}
#[derive(Debug, Clone)]
pub struct ExternDirective {
    pub name_parts: Vec<RustNamePart>,
    pub type_parts: Vec<RustNamePart>,
}
pub type RustNamePart = String;
#[derive(Debug, Clone)]
pub struct EndOfInput;
#[derive(Debug, Clone)]
pub struct Whitespace;
#[derive(Debug, Clone)]
pub struct Comment;
impl peginator_generated::PegParser for Grammar {
    fn parse_advanced<T: peginator_generated::ParseTracer>(
        s: &str,
        settings: &peginator_generated::ParseSettings,
    ) -> Result<Self, peginator_generated::ParseError> {
        Ok(peginator_generated::parse_Grammar(
            peginator_generated::ParseState::new(s, settings),
            T::new(),
            &mut Default::default(),
        )?
        .result)
    }
}
#[allow(
    non_snake_case,
    unused_variables,
    unused_imports,
    unused_mut,
    dead_code
)]
mod peginator_generated {
    use super::*;
    use crate::runtime::*;
    pub use crate::runtime::{
        IndentedTracer, ParseError, ParseSettings, ParseState, ParseTracer, PegParser, PegPosition,
    };
    #[derive(Default)]
    pub struct ParseCache<'a> {
        _please_dont_complain: std::marker::PhantomData<&'a ()>,
    }
    mod Grammar_impl {
        use super::*;
        mod part_0 {
            use super::*;
            mod closure {
                use super::*;
                mod part_0 {
                    use super::*;
                    #[inline(always)]
                    pub fn parse<'a>(
                        mut state: ParseState<'a>,
                        tracer: impl ParseTracer,
                        cache: &mut ParseCache<'a>,
                    ) -> ParseResult<'a, Parsed> {
                        match parse_Whitespace(state.clone(), tracer, cache)
                            .and_then(|ParseOk { state, .. }| parse_Rule(state, tracer, cache))
                            .map_inner(|result| vec![Parsed_rules::Rule(result)])
                        {
                            Ok(ok_result) => return Ok(ok_result),
                            Err(err) => state = state.record_error(err),
                        }
                        match parse_Whitespace(state.clone(), tracer, cache)
                            .and_then(|ParseOk { state, .. }| parse_CharRule(state, tracer, cache))
                            .map_inner(|result| vec![Parsed_rules::CharRule(result)])
                        {
                            Ok(ok_result) => return Ok(ok_result),
                            Err(err) => state = state.record_error(err),
                        }
                        match parse_Whitespace(state.clone(), tracer, cache)
                            .and_then(|ParseOk { state, .. }| {
                                parse_ExternRule(state, tracer, cache)
                            })
                            .map_inner(|result| vec![Parsed_rules::ExternRule(result)])
                        {
                            Ok(ok_result) => return Ok(ok_result),
                            Err(err) => state = state.record_error(err),
                        }
                        Err(state.report_farthest_error())
                    }
                    pub type Parsed = Vec<Parsed_rules>;
                }
                #[inline(always)]
                pub fn parse<'a>(
                    state: ParseState<'a>,
                    tracer: impl ParseTracer,
                    cache: &mut ParseCache<'a>,
                ) -> ParseResult<'a, Parsed> {
                    let ParseOk {
                        result: mut rules,
                        state,
                    } = part_0::parse(state, tracer, cache)?;
                    let ParseOk { state, .. } = parse_Whitespace(state, tracer, cache)
                        .and_then(|ParseOk { state, .. }| parse_character_literal(state, ';'))
                        .discard_result()?;
                    Ok(ParseOk {
                        result: rules,
                        state,
                    })
                }
                pub type Parsed = Vec<Parsed_rules>;
            }
            #[inline(always)]
            pub fn parse<'a>(
                state: ParseState<'a>,
                tracer: impl ParseTracer,
                cache: &mut ParseCache<'a>,
            ) -> ParseResult<'a, Parsed> {
                let mut rules: Vec<Parsed_rules> = Vec::new();
                let mut state = state;
                loop {
                    match closure::parse(state.clone(), tracer, cache) {
                        Ok(ParseOk {
                            result: __result,
                            state: new_state,
                            ..
                        }) => {
                            rules.extend(__result);
                            state = new_state;
                        }
                        Err(err) => {
                            state = state.record_error(err);
                            break;
                        }
                    }
                }
                Ok(ParseOk {
                    result: rules,
                    state,
                })
            }
            pub type Parsed = Vec<Parsed_rules>;
        }
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            let ParseOk {
                result: mut rules,
                state,
            } = part_0::parse(state, tracer, cache)?;
            let ParseOk { state, .. } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_end_of_input(state))?;
            Ok(ParseOk {
                result: rules,
                state,
            })
        }
        pub type Parsed = Vec<Parsed_rules>;
        use super::Grammar_rules as Parsed_rules;
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::Grammar> {
            let result = parse(state, tracer, cache)?.map(|r| super::Grammar { rules: r });
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_Grammar<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, Grammar> {
        tracer.run_traced("Grammar", state, |state, tracer| {
            Grammar_impl::rule_parser(state, tracer, cache)
        })
    }
    mod Rule_impl {
        use super::*;
        mod part_0 {
            use super::*;
            mod closure {
                use super::*;
            }
            #[inline(always)]
            pub fn parse<'a>(
                state: ParseState<'a>,
                tracer: impl ParseTracer,
                cache: &mut ParseCache<'a>,
            ) -> ParseResult<'a, Parsed> {
                let mut directives: Vec<DirectiveExpression> = Vec::new();
                let mut state = state;
                loop {
                    match parse_Whitespace(state.clone(), tracer, cache)
                        .and_then(|ParseOk { state, .. }| {
                            parse_DirectiveExpression(state, tracer, cache)
                        })
                        .map_inner(|result| vec![result])
                    {
                        Ok(ParseOk {
                            result: __result,
                            state: new_state,
                            ..
                        }) => {
                            directives.extend(__result);
                            state = new_state;
                        }
                        Err(err) => {
                            state = state.record_error(err);
                            break;
                        }
                    }
                }
                Ok(ParseOk {
                    result: directives,
                    state,
                })
            }
            pub type Parsed = Vec<DirectiveExpression>;
        }
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            let ParseOk {
                result: mut directives,
                state,
            } = part_0::parse(state, tracer, cache)?;
            let ParseOk {
                result: name,
                state,
            } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_Identifier(state, tracer, cache))?;
            let ParseOk { state, .. } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_character_literal(state, '='))
                .discard_result()?;
            let ParseOk {
                result: definition,
                state,
            } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_Choice(state, tracer, cache))?;
            Ok(ParseOk {
                result: Parsed {
                    directives,
                    name,
                    definition,
                },
                state,
            })
        }
        pub struct Parsed {
            pub directives: Vec<DirectiveExpression>,
            pub name: Identifier,
            pub definition: Choice,
        }
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::Rule> {
            let result = parse(state, tracer, cache)?.map(|r| super::Rule {
                directives: r.directives,
                name: r.name,
                definition: r.definition,
            });
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_Rule<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, Rule> {
        tracer.run_traced("Rule", state, |state, tracer| {
            Rule_impl::rule_parser(state, tracer, cache)
        })
    }
    mod CharRule_impl {
        use super::*;
        mod part_0 {
            use super::*;
            mod closure {
                use super::*;
            }
            #[inline(always)]
            pub fn parse<'a>(
                state: ParseState<'a>,
                tracer: impl ParseTracer,
                cache: &mut ParseCache<'a>,
            ) -> ParseResult<'a, Parsed> {
                let mut directives: Vec<CheckDirective> = Vec::new();
                let mut state = state;
                loop {
                    match parse_Whitespace(state.clone(), tracer, cache)
                        .and_then(|ParseOk { state, .. }| {
                            parse_CheckDirective(state, tracer, cache)
                        })
                        .map_inner(|result| vec![result])
                    {
                        Ok(ParseOk {
                            result: __result,
                            state: new_state,
                            ..
                        }) => {
                            directives.extend(__result);
                            state = new_state;
                        }
                        Err(err) => {
                            state = state.record_error(err);
                            break;
                        }
                    }
                }
                Ok(ParseOk {
                    result: directives,
                    state,
                })
            }
            pub type Parsed = Vec<CheckDirective>;
        }
        mod part_2 {
            use super::*;
            mod closure {
                use super::*;
            }
            #[inline(always)]
            pub fn parse<'a>(
                state: ParseState<'a>,
                tracer: impl ParseTracer,
                cache: &mut ParseCache<'a>,
            ) -> ParseResult<'a, Parsed> {
                let mut directives: Vec<CheckDirective> = Vec::new();
                let mut state = state;
                loop {
                    match parse_Whitespace(state.clone(), tracer, cache)
                        .and_then(|ParseOk { state, .. }| {
                            parse_CheckDirective(state, tracer, cache)
                        })
                        .map_inner(|result| vec![result])
                    {
                        Ok(ParseOk {
                            result: __result,
                            state: new_state,
                            ..
                        }) => {
                            directives.extend(__result);
                            state = new_state;
                        }
                        Err(err) => {
                            state = state.record_error(err);
                            break;
                        }
                    }
                }
                Ok(ParseOk {
                    result: directives,
                    state,
                })
            }
            pub type Parsed = Vec<CheckDirective>;
        }
        mod part_6 {
            use super::*;
            mod closure {
                use super::*;
                #[inline(always)]
                pub fn parse<'a>(
                    state: ParseState<'a>,
                    tracer: impl ParseTracer,
                    cache: &mut ParseCache<'a>,
                ) -> ParseResult<'a, Parsed> {
                    let ParseOk { state, .. } = parse_Whitespace(state, tracer, cache)
                        .and_then(|ParseOk { state, .. }| parse_character_literal(state, '|'))
                        .discard_result()?;
                    let ParseOk {
                        result: mut choices,
                        state,
                    } = parse_Whitespace(state, tracer, cache)
                        .and_then(|ParseOk { state, .. }| parse_CharRulePart(state, tracer, cache))
                        .map_inner(|result| vec![result])?;
                    Ok(ParseOk {
                        result: choices,
                        state,
                    })
                }
                pub type Parsed = Vec<CharRulePart>;
            }
            #[inline(always)]
            pub fn parse<'a>(
                state: ParseState<'a>,
                tracer: impl ParseTracer,
                cache: &mut ParseCache<'a>,
            ) -> ParseResult<'a, Parsed> {
                let mut choices: Vec<CharRulePart> = Vec::new();
                let mut state = state;
                loop {
                    match closure::parse(state.clone(), tracer, cache) {
                        Ok(ParseOk {
                            result: __result,
                            state: new_state,
                            ..
                        }) => {
                            choices.extend(__result);
                            state = new_state;
                        }
                        Err(err) => {
                            state = state.record_error(err);
                            break;
                        }
                    }
                }
                Ok(ParseOk {
                    result: choices,
                    state,
                })
            }
            pub type Parsed = Vec<CharRulePart>;
        }
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            let ParseOk {
                result: mut directives,
                state,
            } = part_0::parse(state, tracer, cache)?;
            let ParseOk { state, .. } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_CharDirective(state, tracer, cache))
                .discard_result()?;
            let ParseOk {
                result: extend_directives_with,
                state,
            } = part_2::parse(state, tracer, cache)?;
            directives.extend(extend_directives_with);
            let ParseOk {
                result: name,
                state,
            } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_Identifier(state, tracer, cache))?;
            let ParseOk { state, .. } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_character_literal(state, '='))
                .discard_result()?;
            let ParseOk {
                result: mut choices,
                state,
            } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_CharRulePart(state, tracer, cache))
                .map_inner(|result| vec![result])?;
            let ParseOk {
                result: extend_choices_with,
                state,
            } = part_6::parse(state, tracer, cache)?;
            choices.extend(extend_choices_with);
            Ok(ParseOk {
                result: Parsed {
                    directives,
                    name,
                    choices,
                },
                state,
            })
        }
        pub struct Parsed {
            pub directives: Vec<CheckDirective>,
            pub name: Identifier,
            pub choices: Vec<CharRulePart>,
        }
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::CharRule> {
            let result = parse(state, tracer, cache)?.map(|r| super::CharRule {
                directives: r.directives,
                name: r.name,
                choices: r.choices,
            });
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_CharRule<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, CharRule> {
        tracer.run_traced("CharRule", state, |state, tracer| {
            CharRule_impl::rule_parser(state, tracer, cache)
        })
    }
    mod CharRulePart_impl {
        use super::*;
        #[inline(always)]
        pub fn parse<'a>(
            mut state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            match parse_Whitespace(state.clone(), tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_CharacterRange(state, tracer, cache))
                .map_inner(Parsed__override::CharacterRange)
            {
                Ok(ok_result) => return Ok(ok_result),
                Err(err) => state = state.record_error(err),
            }
            match parse_Whitespace(state.clone(), tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_CharRangePart(state, tracer, cache))
                .map_inner(Parsed__override::CharRangePart)
            {
                Ok(ok_result) => return Ok(ok_result),
                Err(err) => state = state.record_error(err),
            }
            match parse_Whitespace(state.clone(), tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_Identifier(state, tracer, cache))
                .map_inner(Parsed__override::Identifier)
            {
                Ok(ok_result) => return Ok(ok_result),
                Err(err) => state = state.record_error(err),
            }
            Err(state.report_farthest_error())
        }
        pub type Parsed = Parsed__override;
        use super::CharRulePart as Parsed__override;
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::CharRulePart> {
            let result = parse(state, tracer, cache)?;
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_CharRulePart<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, CharRulePart> {
        tracer.run_traced("CharRulePart", state, |state, tracer| {
            CharRulePart_impl::rule_parser(state, tracer, cache)
        })
    }
    mod ExternRule_impl {
        use super::*;
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            let ParseOk {
                result: directive,
                state,
            } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_ExternDirective(state, tracer, cache))?;
            let ParseOk {
                result: name,
                state,
            } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_Identifier(state, tracer, cache))?;
            Ok(ParseOk {
                result: Parsed { directive, name },
                state,
            })
        }
        pub struct Parsed {
            pub directive: ExternDirective,
            pub name: Identifier,
        }
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::ExternRule> {
            let result = parse(state, tracer, cache)?.map(|r| super::ExternRule {
                directive: r.directive,
                name: r.name,
            });
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_ExternRule<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, ExternRule> {
        tracer.run_traced("ExternRule", state, |state, tracer| {
            ExternRule_impl::rule_parser(state, tracer, cache)
        })
    }
    mod Choice_impl {
        use super::*;
        mod part_1 {
            use super::*;
            mod closure {
                use super::*;
                #[inline(always)]
                pub fn parse<'a>(
                    state: ParseState<'a>,
                    tracer: impl ParseTracer,
                    cache: &mut ParseCache<'a>,
                ) -> ParseResult<'a, Parsed> {
                    let ParseOk { state, .. } = parse_Whitespace(state, tracer, cache)
                        .and_then(|ParseOk { state, .. }| parse_character_literal(state, '|'))
                        .discard_result()?;
                    let ParseOk {
                        result: mut choices,
                        state,
                    } = parse_Whitespace(state, tracer, cache)
                        .and_then(|ParseOk { state, .. }| parse_Sequence(state, tracer, cache))
                        .map_inner(|result| vec![result])?;
                    Ok(ParseOk {
                        result: choices,
                        state,
                    })
                }
                pub type Parsed = Vec<Sequence>;
            }
            #[inline(always)]
            pub fn parse<'a>(
                state: ParseState<'a>,
                tracer: impl ParseTracer,
                cache: &mut ParseCache<'a>,
            ) -> ParseResult<'a, Parsed> {
                let mut choices: Vec<Sequence> = Vec::new();
                let mut state = state;
                loop {
                    match closure::parse(state.clone(), tracer, cache) {
                        Ok(ParseOk {
                            result: __result,
                            state: new_state,
                            ..
                        }) => {
                            choices.extend(__result);
                            state = new_state;
                        }
                        Err(err) => {
                            state = state.record_error(err);
                            break;
                        }
                    }
                }
                Ok(ParseOk {
                    result: choices,
                    state,
                })
            }
            pub type Parsed = Vec<Sequence>;
        }
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            let ParseOk {
                result: mut choices,
                state,
            } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_Sequence(state, tracer, cache))
                .map_inner(|result| vec![result])?;
            let ParseOk {
                result: extend_choices_with,
                state,
            } = part_1::parse(state, tracer, cache)?;
            choices.extend(extend_choices_with);
            Ok(ParseOk {
                result: choices,
                state,
            })
        }
        pub type Parsed = Vec<Sequence>;
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::Choice> {
            let result = parse(state, tracer, cache)?.map(|r| super::Choice { choices: r });
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_Choice<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, Choice> {
        tracer.run_traced("Choice", state, |state, tracer| {
            Choice_impl::rule_parser(state, tracer, cache)
        })
    }
    mod Sequence_impl {
        use super::*;
        mod closure {
            use super::*;
        }
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            let mut parts: Vec<DelimitedExpression> = Vec::new();
            let mut state = state;
            loop {
                match parse_Whitespace(state.clone(), tracer, cache)
                    .and_then(|ParseOk { state, .. }| {
                        parse_DelimitedExpression(state, tracer, cache)
                    })
                    .map_inner(|result| vec![result])
                {
                    Ok(ParseOk {
                        result: __result,
                        state: new_state,
                        ..
                    }) => {
                        parts.extend(__result);
                        state = new_state;
                    }
                    Err(err) => {
                        state = state.record_error(err);
                        break;
                    }
                }
            }
            Ok(ParseOk {
                result: parts,
                state,
            })
        }
        pub type Parsed = Vec<DelimitedExpression>;
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::Sequence> {
            let result = parse(state, tracer, cache)?.map(|r| super::Sequence { parts: r });
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_Sequence<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, Sequence> {
        tracer.run_traced("Sequence", state, |state, tracer| {
            Sequence_impl::rule_parser(state, tracer, cache)
        })
    }
    mod Group_impl {
        use super::*;
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            let ParseOk { state, .. } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_character_literal(state, '('))
                .discard_result()?;
            let ParseOk {
                result: body,
                state,
            } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_Choice(state, tracer, cache))?;
            let ParseOk { state, .. } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_character_literal(state, ')'))
                .discard_result()?;
            Ok(ParseOk {
                result: body,
                state,
            })
        }
        pub type Parsed = Choice;
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::Group> {
            let result = parse(state, tracer, cache)?.map(|r| super::Group { body: r });
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_Group<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, Group> {
        tracer.run_traced("Group", state, |state, tracer| {
            Group_impl::rule_parser(state, tracer, cache)
        })
    }
    mod Optional_impl {
        use super::*;
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            let ParseOk { state, .. } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_character_literal(state, '['))
                .discard_result()?;
            let ParseOk {
                result: body,
                state,
            } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_Choice(state, tracer, cache))?;
            let ParseOk { state, .. } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_character_literal(state, ']'))
                .discard_result()?;
            Ok(ParseOk {
                result: body,
                state,
            })
        }
        pub type Parsed = Choice;
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::Optional> {
            let result = parse(state, tracer, cache)?.map(|r| super::Optional { body: r });
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_Optional<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, Optional> {
        tracer.run_traced("Optional", state, |state, tracer| {
            Optional_impl::rule_parser(state, tracer, cache)
        })
    }
    mod Closure_impl {
        use super::*;
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            let ParseOk { state, .. } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_character_literal(state, '{'))
                .discard_result()?;
            let ParseOk {
                result: body,
                state,
            } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_Choice(state, tracer, cache))?;
            let ParseOk { state, .. } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_character_literal(state, '}'))
                .discard_result()?;
            let ParseOk {
                result: at_least_one,
                state,
            } = parse_Whitespace(state.clone(), tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_AtLeastOneMarker(state, tracer, cache))
                .map_inner(Some)
                .or_else(|err| {
                    Ok(ParseOk {
                        result: Default::default(),
                        state: state.record_error(err),
                    })
                })?;
            Ok(ParseOk {
                result: Parsed { body, at_least_one },
                state,
            })
        }
        pub struct Parsed {
            pub body: Choice,
            pub at_least_one: Option<AtLeastOneMarker>,
        }
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::Closure> {
            let result = parse(state, tracer, cache)?.map(|r| super::Closure {
                body: r.body,
                at_least_one: r.at_least_one,
            });
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_Closure<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, Closure> {
        tracer.run_traced("Closure", state, |state, tracer| {
            Closure_impl::rule_parser(state, tracer, cache)
        })
    }
    mod AtLeastOneMarker_impl {
        use super::*;
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_character_literal(state, '+'))
                .discard_result()
        }
        pub type Parsed = ();
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::AtLeastOneMarker> {
            let result = parse(state, tracer, cache)?.map(|r| super::AtLeastOneMarker {});
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_AtLeastOneMarker<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, AtLeastOneMarker> {
        tracer.run_traced("AtLeastOneMarker", state, |state, tracer| {
            AtLeastOneMarker_impl::rule_parser(state, tracer, cache)
        })
    }
    mod NegativeLookahead_impl {
        use super::*;
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            let ParseOk { state, .. } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_character_literal(state, '!'))
                .discard_result()?;
            let ParseOk {
                result: expr,
                state,
            } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_DelimitedExpression(state, tracer, cache))
                .map_inner(Box::new)?;
            Ok(ParseOk {
                result: expr,
                state,
            })
        }
        pub type Parsed = Box<DelimitedExpression>;
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::NegativeLookahead> {
            let result = parse(state, tracer, cache)?.map(|r| super::NegativeLookahead { expr: r });
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_NegativeLookahead<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, NegativeLookahead> {
        tracer.run_traced("NegativeLookahead", state, |state, tracer| {
            NegativeLookahead_impl::rule_parser(state, tracer, cache)
        })
    }
    mod PositiveLookahead_impl {
        use super::*;
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            let ParseOk { state, .. } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_character_literal(state, '&'))
                .discard_result()?;
            let ParseOk {
                result: expr,
                state,
            } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_DelimitedExpression(state, tracer, cache))
                .map_inner(Box::new)?;
            Ok(ParseOk {
                result: expr,
                state,
            })
        }
        pub type Parsed = Box<DelimitedExpression>;
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::PositiveLookahead> {
            let result = parse(state, tracer, cache)?.map(|r| super::PositiveLookahead { expr: r });
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_PositiveLookahead<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, PositiveLookahead> {
        tracer.run_traced("PositiveLookahead", state, |state, tracer| {
            PositiveLookahead_impl::rule_parser(state, tracer, cache)
        })
    }
    mod CharacterRange_impl {
        use super::*;
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            let ParseOk {
                result: from,
                state,
            } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_CharRangePart(state, tracer, cache))?;
            let ParseOk { state, .. } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_string_literal(state, ".."))
                .discard_result()?;
            let ParseOk { result: to, state } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_CharRangePart(state, tracer, cache))?;
            Ok(ParseOk {
                result: Parsed { from, to },
                state,
            })
        }
        pub struct Parsed {
            pub from: CharRangePart,
            pub to: CharRangePart,
        }
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::CharacterRange> {
            let result = parse(state, tracer, cache)?.map(|r| super::CharacterRange {
                from: r.from,
                to: r.to,
            });
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_CharacterRange<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, CharacterRange> {
        tracer.run_traced("CharacterRange", state, |state, tracer| {
            CharacterRange_impl::rule_parser(state, tracer, cache)
        })
    }
    mod CharRangePart_impl {
        use super::*;
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            let ParseOk { state, .. } = parse_character_literal(state, '\'').discard_result()?;
            let ParseOk {
                result: _override,
                state,
            } = parse_StringItem(state, tracer, cache)?;
            let ParseOk { state, .. } = parse_character_literal(state, '\'').discard_result()?;
            Ok(ParseOk {
                result: _override,
                state,
            })
        }
        pub type Parsed = StringItem;
        use super::CharRangePart as Parsed__override;
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::CharRangePart> {
            let result = parse(state, tracer, cache)?;
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_CharRangePart<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, CharRangePart> {
        tracer.run_traced("CharRangePart", state, |state, tracer| {
            CharRangePart_impl::rule_parser(state, tracer, cache)
        })
    }
    mod StringLiteral_impl {
        use super::*;
        mod part_1 {
            use super::*;
            mod choice_0 {
                use super::*;
                mod part_1 {
                    use super::*;
                    mod closure {
                        use super::*;
                        mod part_0 {
                            use super::*;
                            mod negative_lookahead {
                                use super::*;
                                #[inline(always)]
                                pub fn parse<'a>(
                                    state: ParseState<'a>,
                                    tracer: impl ParseTracer,
                                    cache: &mut ParseCache<'a>,
                                ) -> ParseResult<'a, Parsed> {
                                    parse_character_literal(state, '"').discard_result()
                                }
                                pub type Parsed = ();
                            }
                            #[inline(always)]
                            pub fn parse<'a>(
                                state: ParseState<'a>,
                                tracer: impl ParseTracer,
                                cache: &mut ParseCache<'a>,
                            ) -> ParseResult<'a, Parsed> {
                                match negative_lookahead::parse(state.clone(), tracer, cache) {
                                    Ok(_) => Err(state.report_error(
                                        ParseErrorSpecifics::NegativeLookaheadFailed,
                                    )),
                                    Err(_) => Ok(ParseOk { result: (), state }),
                                }
                            }
                            pub type Parsed = ();
                        }
                        #[inline(always)]
                        pub fn parse<'a>(
                            state: ParseState<'a>,
                            tracer: impl ParseTracer,
                            cache: &mut ParseCache<'a>,
                        ) -> ParseResult<'a, Parsed> {
                            let ParseOk { state, .. } = part_0::parse(state, tracer, cache)?;
                            let ParseOk {
                                result: mut body,
                                state,
                            } = parse_StringItem(state, tracer, cache)
                                .map_inner(|result| vec![result])?;
                            Ok(ParseOk {
                                result: body,
                                state,
                            })
                        }
                        pub type Parsed = Vec<StringItem>;
                    }
                    #[inline(always)]
                    pub fn parse<'a>(
                        state: ParseState<'a>,
                        tracer: impl ParseTracer,
                        cache: &mut ParseCache<'a>,
                    ) -> ParseResult<'a, Parsed> {
                        let mut body: Vec<StringItem> = Vec::new();
                        let mut state = state;
                        loop {
                            match closure::parse(state.clone(), tracer, cache) {
                                Ok(ParseOk {
                                    result: __result,
                                    state: new_state,
                                    ..
                                }) => {
                                    body.extend(__result);
                                    state = new_state;
                                }
                                Err(err) => {
                                    state = state.record_error(err);
                                    break;
                                }
                            }
                        }
                        Ok(ParseOk {
                            result: body,
                            state,
                        })
                    }
                    pub type Parsed = Vec<StringItem>;
                }
                #[inline(always)]
                pub fn parse<'a>(
                    state: ParseState<'a>,
                    tracer: impl ParseTracer,
                    cache: &mut ParseCache<'a>,
                ) -> ParseResult<'a, Parsed> {
                    let ParseOk { state, .. } =
                        parse_character_literal(state, '"').discard_result()?;
                    let ParseOk {
                        result: mut body,
                        state,
                    } = part_1::parse(state, tracer, cache)?;
                    let ParseOk { state, .. } =
                        parse_character_literal(state, '"').discard_result()?;
                    Ok(ParseOk {
                        result: body,
                        state,
                    })
                }
                pub type Parsed = Vec<StringItem>;
            }
            mod choice_1 {
                use super::*;
                mod part_1 {
                    use super::*;
                    mod closure {
                        use super::*;
                        mod part_0 {
                            use super::*;
                            mod negative_lookahead {
                                use super::*;
                                #[inline(always)]
                                pub fn parse<'a>(
                                    state: ParseState<'a>,
                                    tracer: impl ParseTracer,
                                    cache: &mut ParseCache<'a>,
                                ) -> ParseResult<'a, Parsed> {
                                    parse_character_literal(state, '\'').discard_result()
                                }
                                pub type Parsed = ();
                            }
                            #[inline(always)]
                            pub fn parse<'a>(
                                state: ParseState<'a>,
                                tracer: impl ParseTracer,
                                cache: &mut ParseCache<'a>,
                            ) -> ParseResult<'a, Parsed> {
                                match negative_lookahead::parse(state.clone(), tracer, cache) {
                                    Ok(_) => Err(state.report_error(
                                        ParseErrorSpecifics::NegativeLookaheadFailed,
                                    )),
                                    Err(_) => Ok(ParseOk { result: (), state }),
                                }
                            }
                            pub type Parsed = ();
                        }
                        #[inline(always)]
                        pub fn parse<'a>(
                            state: ParseState<'a>,
                            tracer: impl ParseTracer,
                            cache: &mut ParseCache<'a>,
                        ) -> ParseResult<'a, Parsed> {
                            let ParseOk { state, .. } = part_0::parse(state, tracer, cache)?;
                            let ParseOk {
                                result: mut body,
                                state,
                            } = parse_StringItem(state, tracer, cache)
                                .map_inner(|result| vec![result])?;
                            Ok(ParseOk {
                                result: body,
                                state,
                            })
                        }
                        pub type Parsed = Vec<StringItem>;
                    }
                    #[inline(always)]
                    pub fn parse<'a>(
                        state: ParseState<'a>,
                        tracer: impl ParseTracer,
                        cache: &mut ParseCache<'a>,
                    ) -> ParseResult<'a, Parsed> {
                        let mut body: Vec<StringItem> = Vec::new();
                        let mut state = state;
                        loop {
                            match closure::parse(state.clone(), tracer, cache) {
                                Ok(ParseOk {
                                    result: __result,
                                    state: new_state,
                                    ..
                                }) => {
                                    body.extend(__result);
                                    state = new_state;
                                }
                                Err(err) => {
                                    state = state.record_error(err);
                                    break;
                                }
                            }
                        }
                        Ok(ParseOk {
                            result: body,
                            state,
                        })
                    }
                    pub type Parsed = Vec<StringItem>;
                }
                #[inline(always)]
                pub fn parse<'a>(
                    state: ParseState<'a>,
                    tracer: impl ParseTracer,
                    cache: &mut ParseCache<'a>,
                ) -> ParseResult<'a, Parsed> {
                    let ParseOk { state, .. } =
                        parse_character_literal(state, '\'').discard_result()?;
                    let ParseOk {
                        result: mut body,
                        state,
                    } = part_1::parse(state, tracer, cache)?;
                    let ParseOk { state, .. } =
                        parse_character_literal(state, '\'').discard_result()?;
                    Ok(ParseOk {
                        result: body,
                        state,
                    })
                }
                pub type Parsed = Vec<StringItem>;
            }
            #[inline(always)]
            pub fn parse<'a>(
                mut state: ParseState<'a>,
                tracer: impl ParseTracer,
                cache: &mut ParseCache<'a>,
            ) -> ParseResult<'a, Parsed> {
                match choice_0::parse(state.clone(), tracer, cache) {
                    Ok(ok_result) => return Ok(ok_result),
                    Err(err) => state = state.record_error(err),
                }
                match choice_1::parse(state.clone(), tracer, cache) {
                    Ok(ok_result) => return Ok(ok_result),
                    Err(err) => state = state.record_error(err),
                }
                Err(state.report_farthest_error())
            }
            pub type Parsed = Vec<StringItem>;
        }
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            let ParseOk {
                result: insensitive,
                state,
            } = parse_CaseInsensitiveMarker(state.clone(), tracer, cache)
                .map_inner(Some)
                .or_else(|err| {
                    Ok(ParseOk {
                        result: Default::default(),
                        state: state.record_error(err),
                    })
                })?;
            let ParseOk {
                result: mut body,
                state,
            } = part_1::parse(state, tracer, cache)?;
            Ok(ParseOk {
                result: Parsed { insensitive, body },
                state,
            })
        }
        pub struct Parsed {
            pub insensitive: Option<CaseInsensitiveMarker>,
            pub body: Vec<StringItem>,
        }
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::StringLiteral> {
            let result = parse(state, tracer, cache)?.map(|r| super::StringLiteral {
                insensitive: r.insensitive,
                body: r.body,
            });
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_StringLiteral<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, StringLiteral> {
        tracer.run_traced("StringLiteral", state, |state, tracer| {
            StringLiteral_impl::rule_parser(state, tracer, cache)
        })
    }
    mod CaseInsensitiveMarker_impl {
        use super::*;
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_character_literal(state, 'i'))
                .discard_result()
        }
        pub type Parsed = ();
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::CaseInsensitiveMarker> {
            let result = parse(state, tracer, cache)?.map(|r| super::CaseInsensitiveMarker {});
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_CaseInsensitiveMarker<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, CaseInsensitiveMarker> {
        tracer.run_traced("CaseInsensitiveMarker", state, |state, tracer| {
            CaseInsensitiveMarker_impl::rule_parser(state, tracer, cache)
        })
    }
    mod Field_impl {
        use super::*;
        mod part_0 {
            use super::*;
            mod optional {
                use super::*;
                #[inline(always)]
                pub fn parse<'a>(
                    state: ParseState<'a>,
                    tracer: impl ParseTracer,
                    cache: &mut ParseCache<'a>,
                ) -> ParseResult<'a, Parsed> {
                    let ParseOk {
                        result: name,
                        state,
                    } = parse_Whitespace(state, tracer, cache)
                        .and_then(|ParseOk { state, .. }| parse_Identifier(state, tracer, cache))
                        .map_inner(Some)?;
                    let ParseOk { state, .. } = parse_Whitespace(state, tracer, cache)
                        .and_then(|ParseOk { state, .. }| parse_character_literal(state, ':'))
                        .discard_result()?;
                    let ParseOk {
                        result: boxed,
                        state,
                    } = parse_Whitespace(state.clone(), tracer, cache)
                        .and_then(|ParseOk { state, .. }| parse_BoxMarker(state, tracer, cache))
                        .map_inner(Some)
                        .or_else(|err| {
                            Ok(ParseOk {
                                result: Default::default(),
                                state: state.record_error(err),
                            })
                        })?;
                    Ok(ParseOk {
                        result: Parsed { name, boxed },
                        state,
                    })
                }
                pub struct Parsed {
                    pub name: Option<Identifier>,
                    pub boxed: Option<BoxMarker>,
                }
            }
            #[inline(always)]
            pub fn parse<'a>(
                state: ParseState<'a>,
                tracer: impl ParseTracer,
                cache: &mut ParseCache<'a>,
            ) -> ParseResult<'a, Parsed> {
                optional::parse(state.clone(), tracer, cache)
                    .map_inner(|result| Parsed {
                        name: result.name,
                        boxed: result.boxed,
                    })
                    .or_else(|err| {
                        Ok(ParseOk {
                            result: Parsed {
                                name: Default::default(),
                                boxed: Default::default(),
                            },
                            state: state.record_error(err),
                        })
                    })
            }
            pub struct Parsed {
                pub name: Option<Identifier>,
                pub boxed: Option<BoxMarker>,
            }
        }
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            let ParseOk {
                result: part_0::Parsed { name, boxed },
                state,
            } = part_0::parse(state, tracer, cache)?;
            let ParseOk { result: typ, state } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_Identifier(state, tracer, cache))?;
            Ok(ParseOk {
                result: Parsed { name, boxed, typ },
                state,
            })
        }
        pub struct Parsed {
            pub name: Option<Identifier>,
            pub boxed: Option<BoxMarker>,
            pub typ: Identifier,
        }
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::Field> {
            let result = parse(state, tracer, cache)?.map(|r| super::Field {
                name: r.name,
                boxed: r.boxed,
                typ: r.typ,
            });
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_Field<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, Field> {
        tracer.run_traced("Field", state, |state, tracer| {
            Field_impl::rule_parser(state, tracer, cache)
        })
    }
    mod BoxMarker_impl {
        use super::*;
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_character_literal(state, '*'))
                .discard_result()
        }
        pub type Parsed = ();
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::BoxMarker> {
            let result = parse(state, tracer, cache)?.map(|r| super::BoxMarker {});
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_BoxMarker<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, BoxMarker> {
        tracer.run_traced("BoxMarker", state, |state, tracer| {
            BoxMarker_impl::rule_parser(state, tracer, cache)
        })
    }
    mod OverrideField_impl {
        use super::*;
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            let ParseOk { state, .. } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_character_literal(state, '@'))
                .discard_result()?;
            let ParseOk { state, .. } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_character_literal(state, ':'))
                .discard_result()?;
            let ParseOk { result: typ, state } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_Identifier(state, tracer, cache))?;
            Ok(ParseOk { result: typ, state })
        }
        pub type Parsed = Identifier;
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::OverrideField> {
            let result = parse(state, tracer, cache)?.map(|r| super::OverrideField { typ: r });
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_OverrideField<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, OverrideField> {
        tracer.run_traced("OverrideField", state, |state, tracer| {
            OverrideField_impl::rule_parser(state, tracer, cache)
        })
    }
    mod IncludeRule_impl {
        use super::*;
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            let ParseOk { state, .. } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_character_literal(state, '>'))
                .discard_result()?;
            let ParseOk {
                result: rule,
                state,
            } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_Identifier(state, tracer, cache))?;
            Ok(ParseOk {
                result: rule,
                state,
            })
        }
        pub type Parsed = Identifier;
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::IncludeRule> {
            let result = parse(state, tracer, cache)?.map(|r| super::IncludeRule { rule: r });
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_IncludeRule<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, IncludeRule> {
        tracer.run_traced("IncludeRule", state, |state, tracer| {
            IncludeRule_impl::rule_parser(state, tracer, cache)
        })
    }
    mod DelimitedExpression_impl {
        use super::*;
        #[inline(always)]
        pub fn parse<'a>(
            mut state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            match parse_Whitespace(state.clone(), tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_Group(state, tracer, cache))
                .map_inner(Parsed__override::Group)
            {
                Ok(ok_result) => return Ok(ok_result),
                Err(err) => state = state.record_error(err),
            }
            match parse_Whitespace(state.clone(), tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_Optional(state, tracer, cache))
                .map_inner(Parsed__override::Optional)
            {
                Ok(ok_result) => return Ok(ok_result),
                Err(err) => state = state.record_error(err),
            }
            match parse_Whitespace(state.clone(), tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_Closure(state, tracer, cache))
                .map_inner(Parsed__override::Closure)
            {
                Ok(ok_result) => return Ok(ok_result),
                Err(err) => state = state.record_error(err),
            }
            match parse_Whitespace(state.clone(), tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_NegativeLookahead(state, tracer, cache))
                .map_inner(Parsed__override::NegativeLookahead)
            {
                Ok(ok_result) => return Ok(ok_result),
                Err(err) => state = state.record_error(err),
            }
            match parse_Whitespace(state.clone(), tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_PositiveLookahead(state, tracer, cache))
                .map_inner(Parsed__override::PositiveLookahead)
            {
                Ok(ok_result) => return Ok(ok_result),
                Err(err) => state = state.record_error(err),
            }
            match parse_Whitespace(state.clone(), tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_CharacterRange(state, tracer, cache))
                .map_inner(Parsed__override::CharacterRange)
            {
                Ok(ok_result) => return Ok(ok_result),
                Err(err) => state = state.record_error(err),
            }
            match parse_Whitespace(state.clone(), tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_StringLiteral(state, tracer, cache))
                .map_inner(Parsed__override::StringLiteral)
            {
                Ok(ok_result) => return Ok(ok_result),
                Err(err) => state = state.record_error(err),
            }
            match parse_Whitespace(state.clone(), tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_EndOfInput(state, tracer, cache))
                .map_inner(Parsed__override::EndOfInput)
            {
                Ok(ok_result) => return Ok(ok_result),
                Err(err) => state = state.record_error(err),
            }
            match parse_Whitespace(state.clone(), tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_OverrideField(state, tracer, cache))
                .map_inner(Parsed__override::OverrideField)
            {
                Ok(ok_result) => return Ok(ok_result),
                Err(err) => state = state.record_error(err),
            }
            match parse_Whitespace(state.clone(), tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_IncludeRule(state, tracer, cache))
                .map_inner(Parsed__override::IncludeRule)
            {
                Ok(ok_result) => return Ok(ok_result),
                Err(err) => state = state.record_error(err),
            }
            match parse_Whitespace(state.clone(), tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_Field(state, tracer, cache))
                .map_inner(Parsed__override::Field)
            {
                Ok(ok_result) => return Ok(ok_result),
                Err(err) => state = state.record_error(err),
            }
            Err(state.report_farthest_error())
        }
        pub type Parsed = Parsed__override;
        use super::DelimitedExpression as Parsed__override;
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::DelimitedExpression> {
            let result = parse(state, tracer, cache)?;
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_DelimitedExpression<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, DelimitedExpression> {
        tracer.run_traced("DelimitedExpression", state, |state, tracer| {
            DelimitedExpression_impl::rule_parser(state, tracer, cache)
        })
    }
    mod Identifier_impl {
        use super::*;
        mod closure {
            use super::*;
        }
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            let ParseOk {
                result: __result,
                mut state,
                ..
            } = parse_IdentifierChar(state.clone(), tracer, cache).discard_result()?;
            loop {
                match parse_IdentifierChar(state.clone(), tracer, cache).discard_result() {
                    Ok(ParseOk {
                        result: __result,
                        state: new_state,
                        ..
                    }) => {
                        state = new_state;
                    }
                    Err(err) => {
                        state = state.record_error(err);
                        break;
                    }
                }
            }
            Ok(ParseOk { result: (), state })
        }
        pub type Parsed = ();
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Identifier> {
            let result = parse(state.clone(), tracer, cache)?.map_with_state(|_, new_state| {
                let string = state.slice_until(new_state).to_string();
                string
            });
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_Identifier<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, Identifier> {
        tracer.run_traced("Identifier", state, |state, tracer| {
            Identifier_impl::rule_parser(state, tracer, cache)
        })
    }
    #[inline]
    pub(super) fn parse_IdentifierChar<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, IdentifierChar> {
        if let Ok(result) = parse_character_range(state.clone(), 'a', 'z') {
            return Ok(result);
        }
        if let Ok(result) = parse_character_range(state.clone(), 'A', 'Z') {
            return Ok(result);
        }
        if let Ok(result) = parse_character_range(state.clone(), '0', '9') {
            return Ok(result);
        }
        if let Ok(result) = parse_character_literal(state.clone(), '_') {
            return Ok(result);
        }
        Err(
            state.report_error(ParseErrorSpecifics::ExpectedCharacterClass {
                name: "IdentifierChar",
            }),
        )
    }
    mod StringItem_impl {
        use super::*;
        mod choice_0 {
            use super::*;
            mod part_1 {
                use super::*;
                #[inline(always)]
                pub fn parse<'a>(
                    mut state: ParseState<'a>,
                    tracer: impl ParseTracer,
                    cache: &mut ParseCache<'a>,
                ) -> ParseResult<'a, Parsed> {
                    match parse_SimpleEscape(state.clone(), tracer, cache)
                        .map_inner(Parsed__override::SimpleEscape)
                    {
                        Ok(ok_result) => return Ok(ok_result),
                        Err(err) => state = state.record_error(err),
                    }
                    match parse_HexaEscape(state.clone(), tracer, cache)
                        .map_inner(Parsed__override::HexaEscape)
                    {
                        Ok(ok_result) => return Ok(ok_result),
                        Err(err) => state = state.record_error(err),
                    }
                    match parse_Utf8Escape(state.clone(), tracer, cache)
                        .map_inner(Parsed__override::Utf8Escape)
                    {
                        Ok(ok_result) => return Ok(ok_result),
                        Err(err) => state = state.record_error(err),
                    }
                    Err(state.report_farthest_error())
                }
                pub type Parsed = Parsed__override;
            }
            #[inline(always)]
            pub fn parse<'a>(
                state: ParseState<'a>,
                tracer: impl ParseTracer,
                cache: &mut ParseCache<'a>,
            ) -> ParseResult<'a, Parsed> {
                let ParseOk { state, .. } =
                    parse_character_literal(state, '\\').discard_result()?;
                let ParseOk {
                    result: _override,
                    state,
                } = part_1::parse(state, tracer, cache)?;
                Ok(ParseOk {
                    result: _override,
                    state,
                })
            }
            pub type Parsed = Parsed__override;
        }
        mod choice_1 {
            use super::*;
            mod part_0 {
                use super::*;
                mod negative_lookahead {
                    use super::*;
                    #[inline(always)]
                    pub fn parse<'a>(
                        state: ParseState<'a>,
                        tracer: impl ParseTracer,
                        cache: &mut ParseCache<'a>,
                    ) -> ParseResult<'a, Parsed> {
                        parse_character_literal(state, '\\').discard_result()
                    }
                    pub type Parsed = ();
                }
                #[inline(always)]
                pub fn parse<'a>(
                    state: ParseState<'a>,
                    tracer: impl ParseTracer,
                    cache: &mut ParseCache<'a>,
                ) -> ParseResult<'a, Parsed> {
                    match negative_lookahead::parse(state.clone(), tracer, cache) {
                        Ok(_) => {
                            Err(state.report_error(ParseErrorSpecifics::NegativeLookaheadFailed))
                        }
                        Err(_) => Ok(ParseOk { result: (), state }),
                    }
                }
                pub type Parsed = ();
            }
            #[inline(always)]
            pub fn parse<'a>(
                state: ParseState<'a>,
                tracer: impl ParseTracer,
                cache: &mut ParseCache<'a>,
            ) -> ParseResult<'a, Parsed> {
                let ParseOk { state, .. } = part_0::parse(state, tracer, cache)?;
                let ParseOk {
                    result: _override,
                    state,
                } = parse_char(state, tracer, cache).map_inner(Parsed__override::char)?;
                Ok(ParseOk {
                    result: _override,
                    state,
                })
            }
            pub type Parsed = Parsed__override;
        }
        #[inline(always)]
        pub fn parse<'a>(
            mut state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            match choice_0::parse(state.clone(), tracer, cache) {
                Ok(ok_result) => return Ok(ok_result),
                Err(err) => state = state.record_error(err),
            }
            match choice_1::parse(state.clone(), tracer, cache) {
                Ok(ok_result) => return Ok(ok_result),
                Err(err) => state = state.record_error(err),
            }
            Err(state.report_farthest_error())
        }
        pub type Parsed = Parsed__override;
        use super::StringItem as Parsed__override;
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::StringItem> {
            let result = parse(state, tracer, cache)?;
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_StringItem<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, StringItem> {
        tracer.run_traced("StringItem", state, |state, tracer| {
            StringItem_impl::rule_parser(state, tracer, cache)
        })
    }
    mod SimpleEscape_impl {
        use super::*;
        #[inline(always)]
        pub fn parse<'a>(
            mut state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            match parse_SimpleEscapeNewline(state.clone(), tracer, cache)
                .map_inner(Parsed__override::SimpleEscapeNewline)
            {
                Ok(ok_result) => return Ok(ok_result),
                Err(err) => state = state.record_error(err),
            }
            match parse_SimpleEscapeCarriageReturn(state.clone(), tracer, cache)
                .map_inner(Parsed__override::SimpleEscapeCarriageReturn)
            {
                Ok(ok_result) => return Ok(ok_result),
                Err(err) => state = state.record_error(err),
            }
            match parse_SimpleEscapeTab(state.clone(), tracer, cache)
                .map_inner(Parsed__override::SimpleEscapeTab)
            {
                Ok(ok_result) => return Ok(ok_result),
                Err(err) => state = state.record_error(err),
            }
            match parse_SimpleEscapeBackslash(state.clone(), tracer, cache)
                .map_inner(Parsed__override::SimpleEscapeBackslash)
            {
                Ok(ok_result) => return Ok(ok_result),
                Err(err) => state = state.record_error(err),
            }
            match parse_SimpleEscapeQuote(state.clone(), tracer, cache)
                .map_inner(Parsed__override::SimpleEscapeQuote)
            {
                Ok(ok_result) => return Ok(ok_result),
                Err(err) => state = state.record_error(err),
            }
            match parse_SimpleEscapeDQuote(state.clone(), tracer, cache)
                .map_inner(Parsed__override::SimpleEscapeDQuote)
            {
                Ok(ok_result) => return Ok(ok_result),
                Err(err) => state = state.record_error(err),
            }
            Err(state.report_farthest_error())
        }
        pub type Parsed = Parsed__override;
        use super::SimpleEscape as Parsed__override;
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::SimpleEscape> {
            let result = parse(state, tracer, cache)?;
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_SimpleEscape<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, SimpleEscape> {
        tracer.run_traced("SimpleEscape", state, |state, tracer| {
            SimpleEscape_impl::rule_parser(state, tracer, cache)
        })
    }
    mod SimpleEscapeNewline_impl {
        use super::*;
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            parse_character_literal(state, 'n').discard_result()
        }
        pub type Parsed = ();
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::SimpleEscapeNewline> {
            let result = parse(state, tracer, cache)?.map(|r| super::SimpleEscapeNewline {});
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_SimpleEscapeNewline<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, SimpleEscapeNewline> {
        tracer.run_traced("SimpleEscapeNewline", state, |state, tracer| {
            SimpleEscapeNewline_impl::rule_parser(state, tracer, cache)
        })
    }
    mod SimpleEscapeCarriageReturn_impl {
        use super::*;
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            parse_character_literal(state, 'r').discard_result()
        }
        pub type Parsed = ();
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::SimpleEscapeCarriageReturn> {
            let result = parse(state, tracer, cache)?.map(|r| super::SimpleEscapeCarriageReturn {});
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_SimpleEscapeCarriageReturn<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, SimpleEscapeCarriageReturn> {
        tracer.run_traced("SimpleEscapeCarriageReturn", state, |state, tracer| {
            SimpleEscapeCarriageReturn_impl::rule_parser(state, tracer, cache)
        })
    }
    mod SimpleEscapeTab_impl {
        use super::*;
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            parse_character_literal(state, 't').discard_result()
        }
        pub type Parsed = ();
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::SimpleEscapeTab> {
            let result = parse(state, tracer, cache)?.map(|r| super::SimpleEscapeTab {});
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_SimpleEscapeTab<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, SimpleEscapeTab> {
        tracer.run_traced("SimpleEscapeTab", state, |state, tracer| {
            SimpleEscapeTab_impl::rule_parser(state, tracer, cache)
        })
    }
    mod SimpleEscapeBackslash_impl {
        use super::*;
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            parse_character_literal(state, '\\').discard_result()
        }
        pub type Parsed = ();
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::SimpleEscapeBackslash> {
            let result = parse(state, tracer, cache)?.map(|r| super::SimpleEscapeBackslash {});
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_SimpleEscapeBackslash<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, SimpleEscapeBackslash> {
        tracer.run_traced("SimpleEscapeBackslash", state, |state, tracer| {
            SimpleEscapeBackslash_impl::rule_parser(state, tracer, cache)
        })
    }
    mod SimpleEscapeQuote_impl {
        use super::*;
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            parse_character_literal(state, '\'').discard_result()
        }
        pub type Parsed = ();
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::SimpleEscapeQuote> {
            let result = parse(state, tracer, cache)?.map(|r| super::SimpleEscapeQuote {});
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_SimpleEscapeQuote<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, SimpleEscapeQuote> {
        tracer.run_traced("SimpleEscapeQuote", state, |state, tracer| {
            SimpleEscapeQuote_impl::rule_parser(state, tracer, cache)
        })
    }
    mod SimpleEscapeDQuote_impl {
        use super::*;
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            parse_character_literal(state, '"').discard_result()
        }
        pub type Parsed = ();
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::SimpleEscapeDQuote> {
            let result = parse(state, tracer, cache)?.map(|r| super::SimpleEscapeDQuote {});
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_SimpleEscapeDQuote<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, SimpleEscapeDQuote> {
        tracer.run_traced("SimpleEscapeDQuote", state, |state, tracer| {
            SimpleEscapeDQuote_impl::rule_parser(state, tracer, cache)
        })
    }
    mod HexaEscape_impl {
        use super::*;
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            let ParseOk { state, .. } = parse_character_literal(state, 'x').discard_result()?;
            let ParseOk { result: c1, state } = parse_HexChar(state, tracer, cache)?;
            let ParseOk { result: c2, state } = parse_HexChar(state, tracer, cache)?;
            Ok(ParseOk {
                result: Parsed { c1, c2 },
                state,
            })
        }
        pub struct Parsed {
            pub c1: HexChar,
            pub c2: HexChar,
        }
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::HexaEscape> {
            let result =
                parse(state, tracer, cache)?.map(|r| super::HexaEscape { c1: r.c1, c2: r.c2 });
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_HexaEscape<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, HexaEscape> {
        tracer.run_traced("HexaEscape", state, |state, tracer| {
            HexaEscape_impl::rule_parser(state, tracer, cache)
        })
    }
    #[inline]
    pub(super) fn parse_HexChar<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, HexChar> {
        if let Ok(result) = parse_character_range(state.clone(), '0', '9') {
            return Ok(result);
        }
        if let Ok(result) = parse_character_range(state.clone(), 'a', 'f') {
            return Ok(result);
        }
        if let Ok(result) = parse_character_range(state.clone(), 'A', 'F') {
            return Ok(result);
        }
        Err(state.report_error(ParseErrorSpecifics::ExpectedCharacterClass { name: "HexChar" }))
    }
    mod Utf8Escape_impl {
        use super::*;
        mod choice_0 {
            use super::*;
            mod part_3 {
                use super::*;
                mod optional {
                    use super::*;
                    mod part_1 {
                        use super::*;
                        mod optional {
                            use super::*;
                            mod part_1 {
                                use super::*;
                                mod optional {
                                    use super::*;
                                    mod part_1 {
                                        use super::*;
                                        mod optional {
                                            use super::*;
                                            #[inline(always)]
                                            pub fn parse<'a>(
                                                state: ParseState<'a>,
                                                tracer: impl ParseTracer,
                                                cache: &mut ParseCache<'a>,
                                            ) -> ParseResult<'a, Parsed>
                                            {
                                                let ParseOk { result: c5, state } =
                                                    parse_HexChar(state, tracer, cache)
                                                        .map_inner(Some)?;
                                                let ParseOk { result: c6, state } =
                                                    parse_HexChar(state.clone(), tracer, cache)
                                                        .map_inner(Some)
                                                        .or_else(|err| {
                                                            Ok(ParseOk {
                                                                result: Default::default(),
                                                                state: state.record_error(err),
                                                            })
                                                        })?;
                                                Ok(ParseOk {
                                                    result: Parsed { c5, c6 },
                                                    state,
                                                })
                                            }
                                            pub struct Parsed {
                                                pub c5: Option<HexChar>,
                                                pub c6: Option<HexChar>,
                                            }
                                        }
                                        #[inline(always)]
                                        pub fn parse<'a>(
                                            state: ParseState<'a>,
                                            tracer: impl ParseTracer,
                                            cache: &mut ParseCache<'a>,
                                        ) -> ParseResult<'a, Parsed>
                                        {
                                            optional::parse(state.clone(), tracer, cache)
                                                .map_inner(|result| Parsed {
                                                    c5: result.c5,
                                                    c6: result.c6,
                                                })
                                                .or_else(|err| {
                                                    Ok(ParseOk {
                                                        result: Parsed {
                                                            c5: Default::default(),
                                                            c6: Default::default(),
                                                        },
                                                        state: state.record_error(err),
                                                    })
                                                })
                                        }
                                        pub struct Parsed {
                                            pub c5: Option<HexChar>,
                                            pub c6: Option<HexChar>,
                                        }
                                    }
                                    #[inline(always)]
                                    pub fn parse<'a>(
                                        state: ParseState<'a>,
                                        tracer: impl ParseTracer,
                                        cache: &mut ParseCache<'a>,
                                    ) -> ParseResult<'a, Parsed>
                                    {
                                        let ParseOk { result: c4, state } =
                                            parse_HexChar(state, tracer, cache).map_inner(Some)?;
                                        let ParseOk {
                                            result: part_1::Parsed { c5, c6 },
                                            state,
                                        } = part_1::parse(state, tracer, cache)?;
                                        Ok(ParseOk {
                                            result: Parsed { c4, c5, c6 },
                                            state,
                                        })
                                    }
                                    pub struct Parsed {
                                        pub c4: Option<HexChar>,
                                        pub c5: Option<HexChar>,
                                        pub c6: Option<HexChar>,
                                    }
                                }
                                #[inline(always)]
                                pub fn parse<'a>(
                                    state: ParseState<'a>,
                                    tracer: impl ParseTracer,
                                    cache: &mut ParseCache<'a>,
                                ) -> ParseResult<'a, Parsed> {
                                    optional::parse(state.clone(), tracer, cache)
                                        .map_inner(|result| Parsed {
                                            c4: result.c4,
                                            c5: result.c5,
                                            c6: result.c6,
                                        })
                                        .or_else(|err| {
                                            Ok(ParseOk {
                                                result: Parsed {
                                                    c4: Default::default(),
                                                    c5: Default::default(),
                                                    c6: Default::default(),
                                                },
                                                state: state.record_error(err),
                                            })
                                        })
                                }
                                pub struct Parsed {
                                    pub c4: Option<HexChar>,
                                    pub c5: Option<HexChar>,
                                    pub c6: Option<HexChar>,
                                }
                            }
                            #[inline(always)]
                            pub fn parse<'a>(
                                state: ParseState<'a>,
                                tracer: impl ParseTracer,
                                cache: &mut ParseCache<'a>,
                            ) -> ParseResult<'a, Parsed> {
                                let ParseOk { result: c3, state } =
                                    parse_HexChar(state, tracer, cache).map_inner(Some)?;
                                let ParseOk {
                                    result: part_1::Parsed { c4, c5, c6 },
                                    state,
                                } = part_1::parse(state, tracer, cache)?;
                                Ok(ParseOk {
                                    result: Parsed { c3, c4, c5, c6 },
                                    state,
                                })
                            }
                            pub struct Parsed {
                                pub c3: Option<HexChar>,
                                pub c4: Option<HexChar>,
                                pub c5: Option<HexChar>,
                                pub c6: Option<HexChar>,
                            }
                        }
                        #[inline(always)]
                        pub fn parse<'a>(
                            state: ParseState<'a>,
                            tracer: impl ParseTracer,
                            cache: &mut ParseCache<'a>,
                        ) -> ParseResult<'a, Parsed> {
                            optional::parse(state.clone(), tracer, cache)
                                .map_inner(|result| Parsed {
                                    c3: result.c3,
                                    c4: result.c4,
                                    c5: result.c5,
                                    c6: result.c6,
                                })
                                .or_else(|err| {
                                    Ok(ParseOk {
                                        result: Parsed {
                                            c3: Default::default(),
                                            c4: Default::default(),
                                            c5: Default::default(),
                                            c6: Default::default(),
                                        },
                                        state: state.record_error(err),
                                    })
                                })
                        }
                        pub struct Parsed {
                            pub c3: Option<HexChar>,
                            pub c4: Option<HexChar>,
                            pub c5: Option<HexChar>,
                            pub c6: Option<HexChar>,
                        }
                    }
                    #[inline(always)]
                    pub fn parse<'a>(
                        state: ParseState<'a>,
                        tracer: impl ParseTracer,
                        cache: &mut ParseCache<'a>,
                    ) -> ParseResult<'a, Parsed> {
                        let ParseOk { result: c2, state } =
                            parse_HexChar(state, tracer, cache).map_inner(Some)?;
                        let ParseOk {
                            result: part_1::Parsed { c3, c4, c5, c6 },
                            state,
                        } = part_1::parse(state, tracer, cache)?;
                        Ok(ParseOk {
                            result: Parsed { c2, c3, c4, c5, c6 },
                            state,
                        })
                    }
                    pub struct Parsed {
                        pub c2: Option<HexChar>,
                        pub c3: Option<HexChar>,
                        pub c4: Option<HexChar>,
                        pub c5: Option<HexChar>,
                        pub c6: Option<HexChar>,
                    }
                }
                #[inline(always)]
                pub fn parse<'a>(
                    state: ParseState<'a>,
                    tracer: impl ParseTracer,
                    cache: &mut ParseCache<'a>,
                ) -> ParseResult<'a, Parsed> {
                    optional::parse(state.clone(), tracer, cache)
                        .map_inner(|result| Parsed {
                            c2: result.c2,
                            c3: result.c3,
                            c4: result.c4,
                            c5: result.c5,
                            c6: result.c6,
                        })
                        .or_else(|err| {
                            Ok(ParseOk {
                                result: Parsed {
                                    c2: Default::default(),
                                    c3: Default::default(),
                                    c4: Default::default(),
                                    c5: Default::default(),
                                    c6: Default::default(),
                                },
                                state: state.record_error(err),
                            })
                        })
                }
                pub struct Parsed {
                    pub c2: Option<HexChar>,
                    pub c3: Option<HexChar>,
                    pub c4: Option<HexChar>,
                    pub c5: Option<HexChar>,
                    pub c6: Option<HexChar>,
                }
            }
            #[inline(always)]
            pub fn parse<'a>(
                state: ParseState<'a>,
                tracer: impl ParseTracer,
                cache: &mut ParseCache<'a>,
            ) -> ParseResult<'a, Parsed> {
                let ParseOk { state, .. } = parse_character_literal(state, 'u').discard_result()?;
                let ParseOk { state, .. } = parse_character_literal(state, '{').discard_result()?;
                let ParseOk { result: c1, state } = parse_HexChar(state, tracer, cache)?;
                let ParseOk {
                    result: part_3::Parsed { c2, c3, c4, c5, c6 },
                    state,
                } = part_3::parse(state, tracer, cache)?;
                let ParseOk { state, .. } = parse_character_literal(state, '}').discard_result()?;
                Ok(ParseOk {
                    result: Parsed {
                        c1,
                        c2,
                        c3,
                        c4,
                        c5,
                        c6,
                    },
                    state,
                })
            }
            pub struct Parsed {
                pub c1: HexChar,
                pub c2: Option<HexChar>,
                pub c3: Option<HexChar>,
                pub c4: Option<HexChar>,
                pub c5: Option<HexChar>,
                pub c6: Option<HexChar>,
            }
        }
        mod choice_1 {
            use super::*;
            #[inline(always)]
            pub fn parse<'a>(
                state: ParseState<'a>,
                tracer: impl ParseTracer,
                cache: &mut ParseCache<'a>,
            ) -> ParseResult<'a, Parsed> {
                let ParseOk { state, .. } = parse_character_literal(state, 'u').discard_result()?;
                let ParseOk { result: c1, state } = parse_HexChar(state, tracer, cache)?;
                let ParseOk { result: c2, state } =
                    parse_HexChar(state, tracer, cache).map_inner(Some)?;
                let ParseOk { result: c3, state } =
                    parse_HexChar(state, tracer, cache).map_inner(Some)?;
                let ParseOk { result: c4, state } =
                    parse_HexChar(state, tracer, cache).map_inner(Some)?;
                Ok(ParseOk {
                    result: Parsed { c1, c2, c3, c4 },
                    state,
                })
            }
            pub struct Parsed {
                pub c1: HexChar,
                pub c2: Option<HexChar>,
                pub c3: Option<HexChar>,
                pub c4: Option<HexChar>,
            }
        }
        mod choice_2 {
            use super::*;
            #[inline(always)]
            pub fn parse<'a>(
                state: ParseState<'a>,
                tracer: impl ParseTracer,
                cache: &mut ParseCache<'a>,
            ) -> ParseResult<'a, Parsed> {
                let ParseOk { state, .. } = parse_character_literal(state, 'U').discard_result()?;
                let ParseOk { state, .. } = parse_character_literal(state, '0').discard_result()?;
                let ParseOk { state, .. } = parse_character_literal(state, '0').discard_result()?;
                let ParseOk { result: c1, state } = parse_HexChar(state, tracer, cache)?;
                let ParseOk { result: c2, state } =
                    parse_HexChar(state, tracer, cache).map_inner(Some)?;
                let ParseOk { result: c3, state } =
                    parse_HexChar(state, tracer, cache).map_inner(Some)?;
                let ParseOk { result: c4, state } =
                    parse_HexChar(state, tracer, cache).map_inner(Some)?;
                let ParseOk { result: c5, state } =
                    parse_HexChar(state, tracer, cache).map_inner(Some)?;
                let ParseOk { result: c6, state } =
                    parse_HexChar(state, tracer, cache).map_inner(Some)?;
                Ok(ParseOk {
                    result: Parsed {
                        c1,
                        c2,
                        c3,
                        c4,
                        c5,
                        c6,
                    },
                    state,
                })
            }
            pub struct Parsed {
                pub c1: HexChar,
                pub c2: Option<HexChar>,
                pub c3: Option<HexChar>,
                pub c4: Option<HexChar>,
                pub c5: Option<HexChar>,
                pub c6: Option<HexChar>,
            }
        }
        #[inline(always)]
        pub fn parse<'a>(
            mut state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            match choice_0::parse(state.clone(), tracer, cache) {
                Ok(ok_result) => {
                    return Ok(ok_result.map(|r| Parsed {
                        c1: r.c1,
                        c2: r.c2,
                        c3: r.c3,
                        c4: r.c4,
                        c5: r.c5,
                        c6: r.c6,
                    }))
                }
                Err(err) => state = state.record_error(err),
            }
            match choice_1::parse(state.clone(), tracer, cache) {
                Ok(ok_result) => {
                    return Ok(ok_result.map(|r| Parsed {
                        c1: r.c1,
                        c2: r.c2,
                        c3: r.c3,
                        c4: r.c4,
                        c5: None,
                        c6: None,
                    }))
                }
                Err(err) => state = state.record_error(err),
            }
            match choice_2::parse(state.clone(), tracer, cache) {
                Ok(ok_result) => {
                    return Ok(ok_result.map(|r| Parsed {
                        c1: r.c1,
                        c2: r.c2,
                        c3: r.c3,
                        c4: r.c4,
                        c5: r.c5,
                        c6: r.c6,
                    }))
                }
                Err(err) => state = state.record_error(err),
            }
            Err(state.report_farthest_error())
        }
        pub struct Parsed {
            pub c1: HexChar,
            pub c2: Option<HexChar>,
            pub c3: Option<HexChar>,
            pub c4: Option<HexChar>,
            pub c5: Option<HexChar>,
            pub c6: Option<HexChar>,
        }
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::Utf8Escape> {
            let result = parse(state, tracer, cache)?.map(|r| super::Utf8Escape {
                c1: r.c1,
                c2: r.c2,
                c3: r.c3,
                c4: r.c4,
                c5: r.c5,
                c6: r.c6,
            });
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_Utf8Escape<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, Utf8Escape> {
        tracer.run_traced("Utf8Escape", state, |state, tracer| {
            Utf8Escape_impl::rule_parser(state, tracer, cache)
        })
    }
    mod DirectiveExpression_impl {
        use super::*;
        #[inline(always)]
        pub fn parse<'a>(
            mut state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            match parse_Whitespace(state.clone(), tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_StringDirective(state, tracer, cache))
                .map_inner(Parsed__override::StringDirective)
            {
                Ok(ok_result) => return Ok(ok_result),
                Err(err) => state = state.record_error(err),
            }
            match parse_Whitespace(state.clone(), tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_NoSkipWsDirective(state, tracer, cache))
                .map_inner(Parsed__override::NoSkipWsDirective)
            {
                Ok(ok_result) => return Ok(ok_result),
                Err(err) => state = state.record_error(err),
            }
            match parse_Whitespace(state.clone(), tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_ExportDirective(state, tracer, cache))
                .map_inner(Parsed__override::ExportDirective)
            {
                Ok(ok_result) => return Ok(ok_result),
                Err(err) => state = state.record_error(err),
            }
            match parse_Whitespace(state.clone(), tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_PositionDirective(state, tracer, cache))
                .map_inner(Parsed__override::PositionDirective)
            {
                Ok(ok_result) => return Ok(ok_result),
                Err(err) => state = state.record_error(err),
            }
            match parse_Whitespace(state.clone(), tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_MemoizeDirective(state, tracer, cache))
                .map_inner(Parsed__override::MemoizeDirective)
            {
                Ok(ok_result) => return Ok(ok_result),
                Err(err) => state = state.record_error(err),
            }
            match parse_Whitespace(state.clone(), tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_LeftrecDirective(state, tracer, cache))
                .map_inner(Parsed__override::LeftrecDirective)
            {
                Ok(ok_result) => return Ok(ok_result),
                Err(err) => state = state.record_error(err),
            }
            match parse_Whitespace(state.clone(), tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_CheckDirective(state, tracer, cache))
                .map_inner(Parsed__override::CheckDirective)
            {
                Ok(ok_result) => return Ok(ok_result),
                Err(err) => state = state.record_error(err),
            }
            Err(state.report_farthest_error())
        }
        pub type Parsed = Parsed__override;
        use super::DirectiveExpression as Parsed__override;
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::DirectiveExpression> {
            let result = parse(state, tracer, cache)?;
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_DirectiveExpression<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, DirectiveExpression> {
        tracer.run_traced("DirectiveExpression", state, |state, tracer| {
            DirectiveExpression_impl::rule_parser(state, tracer, cache)
        })
    }
    mod StringDirective_impl {
        use super::*;
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_string_literal(state, "@string"))
                .discard_result()
        }
        pub type Parsed = ();
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::StringDirective> {
            let result = parse(state, tracer, cache)?.map(|r| super::StringDirective {});
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_StringDirective<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, StringDirective> {
        tracer.run_traced("StringDirective", state, |state, tracer| {
            StringDirective_impl::rule_parser(state, tracer, cache)
        })
    }
    mod CharDirective_impl {
        use super::*;
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_string_literal(state, "@char"))
                .discard_result()
        }
        pub type Parsed = ();
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::CharDirective> {
            let result = parse(state, tracer, cache)?.map(|r| super::CharDirective {});
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_CharDirective<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, CharDirective> {
        tracer.run_traced("CharDirective", state, |state, tracer| {
            CharDirective_impl::rule_parser(state, tracer, cache)
        })
    }
    mod NoSkipWsDirective_impl {
        use super::*;
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_string_literal(state, "@no_skip_ws"))
                .discard_result()
        }
        pub type Parsed = ();
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::NoSkipWsDirective> {
            let result = parse(state, tracer, cache)?.map(|r| super::NoSkipWsDirective {});
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_NoSkipWsDirective<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, NoSkipWsDirective> {
        tracer.run_traced("NoSkipWsDirective", state, |state, tracer| {
            NoSkipWsDirective_impl::rule_parser(state, tracer, cache)
        })
    }
    mod ExportDirective_impl {
        use super::*;
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_string_literal(state, "@export"))
                .discard_result()
        }
        pub type Parsed = ();
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::ExportDirective> {
            let result = parse(state, tracer, cache)?.map(|r| super::ExportDirective {});
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_ExportDirective<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, ExportDirective> {
        tracer.run_traced("ExportDirective", state, |state, tracer| {
            ExportDirective_impl::rule_parser(state, tracer, cache)
        })
    }
    mod PositionDirective_impl {
        use super::*;
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_string_literal(state, "@position"))
                .discard_result()
        }
        pub type Parsed = ();
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::PositionDirective> {
            let result = parse(state, tracer, cache)?.map(|r| super::PositionDirective {});
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_PositionDirective<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, PositionDirective> {
        tracer.run_traced("PositionDirective", state, |state, tracer| {
            PositionDirective_impl::rule_parser(state, tracer, cache)
        })
    }
    mod MemoizeDirective_impl {
        use super::*;
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_string_literal(state, "@memoize"))
                .discard_result()
        }
        pub type Parsed = ();
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::MemoizeDirective> {
            let result = parse(state, tracer, cache)?.map(|r| super::MemoizeDirective {});
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_MemoizeDirective<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, MemoizeDirective> {
        tracer.run_traced("MemoizeDirective", state, |state, tracer| {
            MemoizeDirective_impl::rule_parser(state, tracer, cache)
        })
    }
    mod LeftrecDirective_impl {
        use super::*;
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_string_literal(state, "@leftrec"))
                .discard_result()
        }
        pub type Parsed = ();
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::LeftrecDirective> {
            let result = parse(state, tracer, cache)?.map(|r| super::LeftrecDirective {});
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_LeftrecDirective<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, LeftrecDirective> {
        tracer.run_traced("LeftrecDirective", state, |state, tracer| {
            LeftrecDirective_impl::rule_parser(state, tracer, cache)
        })
    }
    mod CheckDirective_impl {
        use super::*;
        mod part_3 {
            use super::*;
            mod closure {
                use super::*;
                #[inline(always)]
                pub fn parse<'a>(
                    state: ParseState<'a>,
                    tracer: impl ParseTracer,
                    cache: &mut ParseCache<'a>,
                ) -> ParseResult<'a, Parsed> {
                    let ParseOk { state, .. } = parse_Whitespace(state, tracer, cache)
                        .and_then(|ParseOk { state, .. }| parse_string_literal(state, "::"))
                        .discard_result()?;
                    let ParseOk {
                        result: mut name_parts,
                        state,
                    } = parse_Whitespace(state, tracer, cache)
                        .and_then(|ParseOk { state, .. }| parse_RustNamePart(state, tracer, cache))
                        .map_inner(|result| vec![result])?;
                    Ok(ParseOk {
                        result: name_parts,
                        state,
                    })
                }
                pub type Parsed = Vec<RustNamePart>;
            }
            #[inline(always)]
            pub fn parse<'a>(
                state: ParseState<'a>,
                tracer: impl ParseTracer,
                cache: &mut ParseCache<'a>,
            ) -> ParseResult<'a, Parsed> {
                let mut name_parts: Vec<RustNamePart> = Vec::new();
                let mut state = state;
                loop {
                    match closure::parse(state.clone(), tracer, cache) {
                        Ok(ParseOk {
                            result: __result,
                            state: new_state,
                            ..
                        }) => {
                            name_parts.extend(__result);
                            state = new_state;
                        }
                        Err(err) => {
                            state = state.record_error(err);
                            break;
                        }
                    }
                }
                Ok(ParseOk {
                    result: name_parts,
                    state,
                })
            }
            pub type Parsed = Vec<RustNamePart>;
        }
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            let ParseOk { state, .. } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_string_literal(state, "@check"))
                .discard_result()?;
            let ParseOk { state, .. } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_character_literal(state, '('))
                .discard_result()?;
            let ParseOk {
                result: mut name_parts,
                state,
            } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_RustNamePart(state, tracer, cache))
                .map_inner(|result| vec![result])?;
            let ParseOk {
                result: extend_name_parts_with,
                state,
            } = part_3::parse(state, tracer, cache)?;
            name_parts.extend(extend_name_parts_with);
            let ParseOk { state, .. } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_character_literal(state, ')'))
                .discard_result()?;
            Ok(ParseOk {
                result: name_parts,
                state,
            })
        }
        pub type Parsed = Vec<RustNamePart>;
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::CheckDirective> {
            let result =
                parse(state, tracer, cache)?.map(|r| super::CheckDirective { name_parts: r });
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_CheckDirective<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, CheckDirective> {
        tracer.run_traced("CheckDirective", state, |state, tracer| {
            CheckDirective_impl::rule_parser(state, tracer, cache)
        })
    }
    mod ExternDirective_impl {
        use super::*;
        mod part_3 {
            use super::*;
            mod closure {
                use super::*;
                #[inline(always)]
                pub fn parse<'a>(
                    state: ParseState<'a>,
                    tracer: impl ParseTracer,
                    cache: &mut ParseCache<'a>,
                ) -> ParseResult<'a, Parsed> {
                    let ParseOk { state, .. } = parse_Whitespace(state, tracer, cache)
                        .and_then(|ParseOk { state, .. }| parse_string_literal(state, "::"))
                        .discard_result()?;
                    let ParseOk {
                        result: mut name_parts,
                        state,
                    } = parse_Whitespace(state, tracer, cache)
                        .and_then(|ParseOk { state, .. }| parse_RustNamePart(state, tracer, cache))
                        .map_inner(|result| vec![result])?;
                    Ok(ParseOk {
                        result: name_parts,
                        state,
                    })
                }
                pub type Parsed = Vec<RustNamePart>;
            }
            #[inline(always)]
            pub fn parse<'a>(
                state: ParseState<'a>,
                tracer: impl ParseTracer,
                cache: &mut ParseCache<'a>,
            ) -> ParseResult<'a, Parsed> {
                let mut name_parts: Vec<RustNamePart> = Vec::new();
                let mut state = state;
                loop {
                    match closure::parse(state.clone(), tracer, cache) {
                        Ok(ParseOk {
                            result: __result,
                            state: new_state,
                            ..
                        }) => {
                            name_parts.extend(__result);
                            state = new_state;
                        }
                        Err(err) => {
                            state = state.record_error(err);
                            break;
                        }
                    }
                }
                Ok(ParseOk {
                    result: name_parts,
                    state,
                })
            }
            pub type Parsed = Vec<RustNamePart>;
        }
        mod part_4 {
            use super::*;
            mod optional {
                use super::*;
                mod part_2 {
                    use super::*;
                    mod closure {
                        use super::*;
                        #[inline(always)]
                        pub fn parse<'a>(
                            state: ParseState<'a>,
                            tracer: impl ParseTracer,
                            cache: &mut ParseCache<'a>,
                        ) -> ParseResult<'a, Parsed> {
                            let ParseOk { state, .. } = parse_Whitespace(state, tracer, cache)
                                .and_then(|ParseOk { state, .. }| parse_string_literal(state, "::"))
                                .discard_result()?;
                            let ParseOk {
                                result: mut type_parts,
                                state,
                            } = parse_Whitespace(state, tracer, cache)
                                .and_then(|ParseOk { state, .. }| {
                                    parse_RustNamePart(state, tracer, cache)
                                })
                                .map_inner(|result| vec![result])?;
                            Ok(ParseOk {
                                result: type_parts,
                                state,
                            })
                        }
                        pub type Parsed = Vec<RustNamePart>;
                    }
                    #[inline(always)]
                    pub fn parse<'a>(
                        state: ParseState<'a>,
                        tracer: impl ParseTracer,
                        cache: &mut ParseCache<'a>,
                    ) -> ParseResult<'a, Parsed> {
                        let mut type_parts: Vec<RustNamePart> = Vec::new();
                        let mut state = state;
                        loop {
                            match closure::parse(state.clone(), tracer, cache) {
                                Ok(ParseOk {
                                    result: __result,
                                    state: new_state,
                                    ..
                                }) => {
                                    type_parts.extend(__result);
                                    state = new_state;
                                }
                                Err(err) => {
                                    state = state.record_error(err);
                                    break;
                                }
                            }
                        }
                        Ok(ParseOk {
                            result: type_parts,
                            state,
                        })
                    }
                    pub type Parsed = Vec<RustNamePart>;
                }
                #[inline(always)]
                pub fn parse<'a>(
                    state: ParseState<'a>,
                    tracer: impl ParseTracer,
                    cache: &mut ParseCache<'a>,
                ) -> ParseResult<'a, Parsed> {
                    let ParseOk { state, .. } = parse_Whitespace(state, tracer, cache)
                        .and_then(|ParseOk { state, .. }| parse_string_literal(state, "->"))
                        .discard_result()?;
                    let ParseOk {
                        result: mut type_parts,
                        state,
                    } = parse_Whitespace(state, tracer, cache)
                        .and_then(|ParseOk { state, .. }| parse_RustNamePart(state, tracer, cache))
                        .map_inner(|result| vec![result])?;
                    let ParseOk {
                        result: extend_type_parts_with,
                        state,
                    } = part_2::parse(state, tracer, cache)?;
                    type_parts.extend(extend_type_parts_with);
                    Ok(ParseOk {
                        result: type_parts,
                        state,
                    })
                }
                pub type Parsed = Vec<RustNamePart>;
            }
            #[inline(always)]
            pub fn parse<'a>(
                state: ParseState<'a>,
                tracer: impl ParseTracer,
                cache: &mut ParseCache<'a>,
            ) -> ParseResult<'a, Parsed> {
                optional::parse(state.clone(), tracer, cache).or_else(|err| {
                    Ok(ParseOk {
                        result: Default::default(),
                        state: state.record_error(err),
                    })
                })
            }
            pub type Parsed = Vec<RustNamePart>;
        }
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            let ParseOk { state, .. } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_string_literal(state, "@extern"))
                .discard_result()?;
            let ParseOk { state, .. } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_character_literal(state, '('))
                .discard_result()?;
            let ParseOk {
                result: mut name_parts,
                state,
            } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_RustNamePart(state, tracer, cache))
                .map_inner(|result| vec![result])?;
            let ParseOk {
                result: extend_name_parts_with,
                state,
            } = part_3::parse(state, tracer, cache)?;
            name_parts.extend(extend_name_parts_with);
            let ParseOk {
                result: mut type_parts,
                state,
            } = part_4::parse(state, tracer, cache)?;
            let ParseOk { state, .. } = parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_character_literal(state, ')'))
                .discard_result()?;
            Ok(ParseOk {
                result: Parsed {
                    name_parts,
                    type_parts,
                },
                state,
            })
        }
        pub struct Parsed {
            pub name_parts: Vec<RustNamePart>,
            pub type_parts: Vec<RustNamePart>,
        }
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::ExternDirective> {
            let result = parse(state, tracer, cache)?.map(|r| super::ExternDirective {
                name_parts: r.name_parts,
                type_parts: r.type_parts,
            });
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_ExternDirective<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, ExternDirective> {
        tracer.run_traced("ExternDirective", state, |state, tracer| {
            ExternDirective_impl::rule_parser(state, tracer, cache)
        })
    }
    mod RustNamePart_impl {
        use super::*;
        mod closure {
            use super::*;
            mod part_0 {
                use super::*;
                mod negative_lookahead {
                    use super::*;
                    #[inline(always)]
                    pub fn parse<'a>(
                        mut state: ParseState<'a>,
                        tracer: impl ParseTracer,
                        cache: &mut ParseCache<'a>,
                    ) -> ParseResult<'a, Parsed> {
                        match parse_Whitespace(state.clone(), tracer, cache)
                            .and_then(|ParseOk { state, .. }| parse_character_literal(state, '-'))
                            .discard_result()
                        {
                            Ok(ok_result) => return Ok(ok_result),
                            Err(err) => state = state.record_error(err),
                        }
                        match parse_Whitespace(state.clone(), tracer, cache)
                            .and_then(|ParseOk { state, .. }| parse_character_literal(state, ')'))
                            .discard_result()
                        {
                            Ok(ok_result) => return Ok(ok_result),
                            Err(err) => state = state.record_error(err),
                        }
                        match parse_Whitespace(state.clone(), tracer, cache)
                            .and_then(|ParseOk { state, .. }| parse_character_literal(state, ':'))
                            .discard_result()
                        {
                            Ok(ok_result) => return Ok(ok_result),
                            Err(err) => state = state.record_error(err),
                        }
                        Err(state.report_farthest_error())
                    }
                    pub type Parsed = ();
                }
                #[inline(always)]
                pub fn parse<'a>(
                    state: ParseState<'a>,
                    tracer: impl ParseTracer,
                    cache: &mut ParseCache<'a>,
                ) -> ParseResult<'a, Parsed> {
                    match negative_lookahead::parse(state.clone(), tracer, cache) {
                        Ok(_) => {
                            Err(state.report_error(ParseErrorSpecifics::NegativeLookaheadFailed))
                        }
                        Err(_) => Ok(ParseOk { result: (), state }),
                    }
                }
                pub type Parsed = ();
            }
            #[inline(always)]
            pub fn parse<'a>(
                state: ParseState<'a>,
                tracer: impl ParseTracer,
                cache: &mut ParseCache<'a>,
            ) -> ParseResult<'a, Parsed> {
                let ParseOk { state, .. } = part_0::parse(state, tracer, cache)?;
                let ParseOk { state, .. } = parse_Whitespace(state, tracer, cache)
                    .and_then(|ParseOk { state, .. }| parse_char(state, tracer, cache))
                    .discard_result()?;
                Ok(ParseOk { result: (), state })
            }
            pub type Parsed = ();
        }
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            let ParseOk {
                result: __result,
                mut state,
                ..
            } = closure::parse(state.clone(), tracer, cache)?;
            loop {
                match closure::parse(state.clone(), tracer, cache) {
                    Ok(ParseOk {
                        result: __result,
                        state: new_state,
                        ..
                    }) => {
                        state = new_state;
                    }
                    Err(err) => {
                        state = state.record_error(err);
                        break;
                    }
                }
            }
            Ok(ParseOk { result: (), state })
        }
        pub type Parsed = ();
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, RustNamePart> {
            let result = parse(state.clone(), tracer, cache)?.map_with_state(|_, new_state| {
                let string = state.slice_until(new_state).to_string();
                string
            });
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_RustNamePart<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, RustNamePart> {
        tracer.run_traced("RustNamePart", state, |state, tracer| {
            RustNamePart_impl::rule_parser(state, tracer, cache)
        })
    }
    mod EndOfInput_impl {
        use super::*;
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            parse_Whitespace(state, tracer, cache)
                .and_then(|ParseOk { state, .. }| parse_character_literal(state, '$'))
                .discard_result()
        }
        pub type Parsed = ();
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::EndOfInput> {
            let result = parse(state, tracer, cache)?.map(|r| super::EndOfInput {});
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_EndOfInput<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, EndOfInput> {
        tracer.run_traced("EndOfInput", state, |state, tracer| {
            EndOfInput_impl::rule_parser(state, tracer, cache)
        })
    }
    mod Whitespace_impl {
        use super::*;
        mod closure {
            use super::*;
            #[inline(always)]
            pub fn parse<'a>(
                mut state: ParseState<'a>,
                tracer: impl ParseTracer,
                cache: &mut ParseCache<'a>,
            ) -> ParseResult<'a, Parsed> {
                match parse_Comment(state.clone(), tracer, cache).discard_result() {
                    Ok(ok_result) => return Ok(ok_result),
                    Err(err) => state = state.record_error(err),
                }
                match parse_character_literal(state.clone(), '\t').discard_result() {
                    Ok(ok_result) => return Ok(ok_result),
                    Err(err) => state = state.record_error(err),
                }
                match parse_character_literal(state.clone(), '\n').discard_result() {
                    Ok(ok_result) => return Ok(ok_result),
                    Err(err) => state = state.record_error(err),
                }
                match parse_character_literal(state.clone(), '\u{c}').discard_result() {
                    Ok(ok_result) => return Ok(ok_result),
                    Err(err) => state = state.record_error(err),
                }
                match parse_character_literal(state.clone(), '\r').discard_result() {
                    Ok(ok_result) => return Ok(ok_result),
                    Err(err) => state = state.record_error(err),
                }
                match parse_character_literal(state.clone(), ' ').discard_result() {
                    Ok(ok_result) => return Ok(ok_result),
                    Err(err) => state = state.record_error(err),
                }
                Err(state.report_farthest_error())
            }
            pub type Parsed = ();
        }
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            let mut state = state;
            loop {
                match closure::parse(state.clone(), tracer, cache) {
                    Ok(ParseOk {
                        result: __result,
                        state: new_state,
                        ..
                    }) => {
                        state = new_state;
                    }
                    Err(err) => {
                        state = state.record_error(err);
                        break;
                    }
                }
            }
            Ok(ParseOk { result: (), state })
        }
        pub type Parsed = ();
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::Whitespace> {
            let result = parse(state, tracer, cache)?.map(|r| super::Whitespace {});
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_Whitespace<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, Whitespace> {
        tracer.run_traced("Whitespace", state, |state, tracer| {
            Whitespace_impl::rule_parser(state, tracer, cache)
        })
    }
    mod Comment_impl {
        use super::*;
        mod part_1 {
            use super::*;
            mod closure {
                use super::*;
                mod part_0 {
                    use super::*;
                    mod negative_lookahead {
                        use super::*;
                        #[inline(always)]
                        pub fn parse<'a>(
                            state: ParseState<'a>,
                            tracer: impl ParseTracer,
                            cache: &mut ParseCache<'a>,
                        ) -> ParseResult<'a, Parsed> {
                            parse_character_literal(state, '\n').discard_result()
                        }
                        pub type Parsed = ();
                    }
                    #[inline(always)]
                    pub fn parse<'a>(
                        state: ParseState<'a>,
                        tracer: impl ParseTracer,
                        cache: &mut ParseCache<'a>,
                    ) -> ParseResult<'a, Parsed> {
                        match negative_lookahead::parse(state.clone(), tracer, cache) {
                            Ok(_) => {
                                Err(state
                                    .report_error(ParseErrorSpecifics::NegativeLookaheadFailed))
                            }
                            Err(_) => Ok(ParseOk { result: (), state }),
                        }
                    }
                    pub type Parsed = ();
                }
                #[inline(always)]
                pub fn parse<'a>(
                    state: ParseState<'a>,
                    tracer: impl ParseTracer,
                    cache: &mut ParseCache<'a>,
                ) -> ParseResult<'a, Parsed> {
                    let ParseOk { state, .. } = part_0::parse(state, tracer, cache)?;
                    let ParseOk { state, .. } =
                        parse_char(state, tracer, cache).discard_result()?;
                    Ok(ParseOk { result: (), state })
                }
                pub type Parsed = ();
            }
            #[inline(always)]
            pub fn parse<'a>(
                state: ParseState<'a>,
                tracer: impl ParseTracer,
                cache: &mut ParseCache<'a>,
            ) -> ParseResult<'a, Parsed> {
                let mut state = state;
                loop {
                    match closure::parse(state.clone(), tracer, cache) {
                        Ok(ParseOk {
                            result: __result,
                            state: new_state,
                            ..
                        }) => {
                            state = new_state;
                        }
                        Err(err) => {
                            state = state.record_error(err);
                            break;
                        }
                    }
                }
                Ok(ParseOk { result: (), state })
            }
            pub type Parsed = ();
        }
        #[inline(always)]
        pub fn parse<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, Parsed> {
            let ParseOk { state, .. } = parse_character_literal(state, '#').discard_result()?;
            let ParseOk { state, .. } = part_1::parse(state, tracer, cache)?;
            let ParseOk { state, .. } = parse_character_literal(state, '\n').discard_result()?;
            Ok(ParseOk { result: (), state })
        }
        pub type Parsed = ();
        #[inline(always)]
        pub fn rule_parser<'a>(
            state: ParseState<'a>,
            tracer: impl ParseTracer,
            cache: &mut ParseCache<'a>,
        ) -> ParseResult<'a, super::Comment> {
            let result = parse(state, tracer, cache)?.map(|r| super::Comment {});
            Ok(result)
        }
    }
    #[inline]
    pub(super) fn parse_Comment<'a>(
        state: ParseState<'a>,
        tracer: impl ParseTracer,
        cache: &mut ParseCache<'a>,
    ) -> ParseResult<'a, Comment> {
        tracer.run_traced("Comment", state, |state, tracer| {
            Comment_impl::rule_parser(state, tracer, cache)
        })
    }
}
